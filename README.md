# dumbo

Some clojure code for putting all your tweets in a Postgres database.

## Requirements

- PostgreSQL

## Setup

Create the database:

```
$ createdb dumbo
```

## Usage

```
$ lein run ~/tweet-data.csv
```
